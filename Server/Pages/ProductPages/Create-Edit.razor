@page "/Product/Create"
@page "/Product/Edit/{id:int}"
@using System.IO
@using Business.IRepository.IApplicationUserRepositories.EmoloyeeIRepositories
@using Business.IRepository.IEstablishmentRepositories
@using Business.IRepository.IProductRepositories
@using Business.IRepository.ISharedRepository
@using DataAcesss.Data.EmployeeModels
@using DataAcesss.Data.EstablishmentModels
@using DataAcesss.Data.ProductModels
@using DataAcesss.Data.Shared
@using Microsoft.AspNetCore.Http
@using Microsoft.AspNetCore.Identity
@using System.ComponentModel.DataAnnotations
@inject NavigationManager NavigationManager
@inject IProductRepository ProductRepository
@inject IProductTypeRepository ProductTypeRepository
@inject IEstablishmentRepository EstablishmentRepository
@inject IProductImageRepository ProductImageRepository
@inject IEstablishment_ProductRepository Establishment_ProductRepository
@inject IEmployeeRepository EmployeeRepository
@inject IFileUploadRepository FileUploadRepository
@inject NavigationManager NavigationManager
@inject UserManager<ApplicationUser> userManager
@inject SignInManager<ApplicationUser> signInManager
@inject RoleManager<IdentityRole> roleManager
@inject AuthenticationStateProvider AuthenticationStateProvider
@attribute [Authorize(Roles ="Manager, Employee")]
<style>
    .product-image{
        display: block;
        width: 100%;
        height: 150px;
        background-size: cover !important;
        border: 3px solid green;
        position: relative;
    }
    .product-image-title{
        position:absolute;
        top:0;
        right:0;
        background-color:green;
        color:white;
        padding:0px 6px;
        display:inline-block;
    }
</style>
<div class="row mt-2 mb-5">
    <h3 class="card-title text-info mb-3 ml-3">@title Product</h3>
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <EditForm Model="inputModel" OnValidSubmit="Submit">
                    <DataAnnotationsValidator/>
                    <ValidationSummary/>
                    <div class="form-group">
                        <label>Product Name</label>
                        <InputText @bind-Value="inputModel.ProductName" class="form-control" reqired></InputText>
                        <ValidationMessage For="() => inputModel.ProductName"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Product Description</label>
                        <InputTextArea @bind-Value="inputModel.ProductDescription" class="form-control" reqired></InputTextArea>
                        <ValidationMessage For="() => inputModel.ProductDescription"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Product Types</label>
                        <InputSelect @bind-Value="inputModel.ProductTypeId" class="form-control" reqired>
                            @if (productTypes != null && productTypes.Any())
                            {
                                <option value="" disabled selected>Choose Type</option>
                                foreach (var productType in productTypes)
                                {
                                    <option value="@productType.ProductTypeId">@productType.Type</option>
                                }

                            }
                            else
                            {
                                <option value="" disabled>No Product Type</option>
                            }
                        </InputSelect>
                        <ValidationMessage For="() => inputModel.ProductTypeId"></ValidationMessage>
                    </div>
                    @if(IsAdmin)
                    {
                        <div class="form-group">
                            <label>Establishments</label>
                            <InputSelect @bind-Value="inputModel.EstablishmentId" class="form-control" reqired>
                                @if (establishments != null && establishments.Any())
                                {
                                    <option value="" disabled selected>Choose Establishment</option>
                                    foreach (var establishment in establishments)
                                    {
                                        <option value="@establishment.EstablishmentId">@establishment.EstablishmentName</option>
                                    }

                                }
                                else
                                {
                                    <option value="" disabled>No Establishments</option>
                                }
                            </InputSelect>
                            <ValidationMessage For="() => inputModel.EstablishmentId"></ValidationMessage>
                        </div>
                    }
                    <div class="form-group">
                        <label>Product Quantity</label>
                        <InputNumber @bind-Value="inputModel.Quantity" class="form-control" reqired></InputNumber>
                        <ValidationMessage For="() => inputModel.Quantity"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Purchase Price</label>
                        <InputNumber @bind-Value="inputModel.PurchasePrice" class="form-control" reqired></InputNumber>
                        <ValidationMessage For="() => inputModel.PurchasePrice"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Retail Price</label>
                        <InputNumber @bind-Value="inputModel.RetailPrice" class="form-control" reqired></InputNumber>
                        <ValidationMessage For="() => inputModel.RetailPrice"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Aid Amount in %</label>
                        
                        <div class="row col-12">
                            <InputNumber @bind-Value="inputModel.AidAmount" class="form-control col-11" reqired></InputNumber>
                            <InputCheckbox @bind-Value="inputModel.Percentage" class="form-control col-1"></InputCheckbox>
                        </div>
                        <ValidationMessage For="() => inputModel.AidAmount"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Production Date</label>
                        <InputDate @bind-Value="inputModel.ProductionDate" class="form-control" reqired></InputDate>
                        <ValidationMessage For="() => inputModel.ProductionDate"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Expiration Date</label>
                        <InputDate @bind-Value="inputModel.ExpirationDate" class="form-control" reqired></InputDate>
                        <ValidationMessage For="() => inputModel.ExpirationDate"></ValidationMessage>
                    </div>
                    <div class="form-group">
                        <label>Product Images</label>
                        <InputFile class="form-control" OnChange="UploadImages"></InputFile>
                        <div class="row">
                            @if (Images != null && Images.Count() > 0)
                            {
                                int name = 1;
                                foreach(var image in Images)
                                {
                                    <div class="col-md-2 mt-3">
                                        <div class="product-image" style="background: url(@image) 50% 50%; border-radius:5px;">
                                            <span class="product-image-title">@name</span>
                                        </div>
                                        <button type="button" class="btn btn-outline-danger btn-block mt-4" @onclick="()=>RemoveImage(image)">Delete</button>
                                    </div>
                                    name++;
                                }
                            }
                        </div>
                    </div>
                    <div class="form-group">
                        <button class="btn btn-success col mb-1" type="submit">@title</button>
                        <button class="btn btn-danger col mb-1" type="reset">Reset</button>
                        <NavLink href="@($"ProductType")" class="btn btn-primary">Back to List</NavLink>
                    </div>
                </EditForm>
            </div>
        </div>
    </div>
</div>

@code {
    public class InputModel
    {
        public int ProductId { get; set; }
        [Required]
        public string ProductName { get; set; }
        public string ProductDescription { get; set; }
        [Required, Range(1,double.MaxValue,ErrorMessage = "Please choose a type")]
        public int ProductTypeId { get; set; }
        [Required, Range(1,double.MaxValue,ErrorMessage = "Please choose an establishment")]
        public int EstablishmentId { get; set; }
        [Required, Range(0,double.MaxValue,ErrorMessage = "This field must be a positive number")]
        public float Quantity { get; set; }
        [Required, Range(0,double.MaxValue,ErrorMessage = "This field must be a positive number")]
        public decimal PurchasePrice { get; set; }
        [Required, Range(0,double.MaxValue,ErrorMessage = "This field must be a positive number")]
        public decimal RetailPrice { get; set; }
        [Required]
        public bool Percentage { get; set; }
        [Required, Range(0,double.MaxValue,ErrorMessage = "This field must be a positive number")]
        public decimal AidAmount { get; set; }
        [Required, DataType(DataType.Date)]
        public DateTime ProductionDate { get; set; }
        [Required ,DataType(DataType.Date)]
        public DateTime ExpirationDate { get; set; }
    }

    public string title { get; set; } = "Create";
    private bool IsAdmin { get; set; }

    [Parameter]
    public int? id { get; set; }
    private InputModel inputModel { get; set; } = new();
    private Product product { get; set; } = new();
    private List<string> Images { get; set; } = new();
    private Establishment_Product establishment_Product { get; set; } = new();
    private ICollection<ProductType> productTypes { get; set; }
    private ICollection<Establishment> establishments { get; set; }
    protected override async Task OnInitializedAsync()
    {
        var user = AuthenticationStateProvider.GetAuthenticationStateAsync().Result.User;
        Employee employee = new();
        if(user.IsInRole("Manager") || user.IsInRole("Employee"))
        {
            IsAdmin = false;
            employee = (Employee)userManager.GetUserAsync(user).Result;
            inputModel.EstablishmentId = employee.EstablishmentId;
        }
        else if(user.IsInRole("Admin"))
        {
            IsAdmin = true;
            establishments = EstablishmentRepository.GetAllEstablishments();
        }

        productTypes = await ProductTypeRepository.GetAllProductsType();

        if(product.ProductImages != null)
        {
            foreach(var productImage in product.ProductImages)
            {
                Images.Add(productImage.ProductImageUrl);
            }
        }

        if(id != null)
        {
            title = "Edit";
            product = await ProductRepository.GetProduct(id.Value);
            if(product.ProductImages != null)
            {
                foreach(var productImage in product.ProductImages)
                {
                    Images.Add(productImage.ProductImageUrl);
                }
            }
            if(employee != null)
                establishment_Product = product.Establishment_Products.FirstOrDefault(x => x.EstablishmentId == employee.EstablishmentId);
            inputModel = new()
            {
                ProductId = product.ProductId,
                ProductName = product.ProductName,
                ProductDescription = product.ProductDescription,
                ProductTypeId = product.ProductTypeId,
                EstablishmentId = establishment_Product.EstablishmentId,
                PurchasePrice = establishment_Product.PurchasePrice,
                RetailPrice = establishment_Product.RetailPrice,
                Quantity = establishment_Product.Quantity,
                AidAmount = establishment_Product.AidAmount,
                Percentage = establishment_Product.Percentage,
                ProductionDate = establishment_Product.ProductionDate,
                ExpirationDate = establishment_Product.ExpirationDate
            };
        }
    }
    private async Task UploadImages(InputFileChangeEventArgs e)
    {

        if(e.GetMultipleFiles().Any())
        {
            foreach(var file in e.GetMultipleFiles())
            {
                FileInfo fileInfo = new FileInfo(file.Name);
                if(fileInfo.Extension.ToLower() == ".jpg" || 
                   fileInfo.Extension.ToLower() == ".jpeg" || 
                   fileInfo.Extension.ToLower() == ".png" )
                {
                    var uploadImage = await FileUploadRepository.UploadImage("Product", file);
                    Images.Add(uploadImage);
                }
            }
        }
    }
    private void RemoveImage(string url)
    {
        var Img = Images.FirstOrDefault(x => x == url);
        Images.Remove(Img);
    }
    private void DeleteImage(string img)
    {
        FileUploadRepository.DeleteImage("Product", img);
    }
    private async Task UpdateProductImage(Product product, List<string> images)
    {
        if(product.ProductImages == null)
        {
            product.ProductImages = new List<ProductImage>();
        }
        foreach (var imageUrl in images)
        {
            if(!product.ProductImages.Any()|| product.ProductImages.Where(x => x.ProductImageUrl == imageUrl) == null)
            {
                ProductImage Image = new ProductImage()
                {
                    ProductId = product.ProductId,
                    Product = product,
                    ProductImageUrl = imageUrl
                };
                await ProductImageRepository.CreateProductImage(Image);
                product.ProductImages.Add(Image);
            }
        }
        foreach (var image in product.ProductImages)
        {
            if(images.Where(x => x == image.ProductImageUrl) == null)
            {
                ProductImageRepository.DeleteProductImageByImageId(image.ProductImageId);
                DeleteImage(image.ProductImageUrl);
            }
        }
    }
    private async Task Submit()
    {
        product = new()
        {
            ProductId = inputModel.ProductId,
            ProductName = inputModel.ProductName,
            ProductDescription = inputModel.ProductDescription,
            ProductTypeId = inputModel.ProductTypeId
        };
        establishment_Product = new()
        {
            EstablishmentId = inputModel.EstablishmentId,
            ProductId = inputModel.ProductId,
            Quantity = inputModel.Quantity,
            PurchasePrice = inputModel.PurchasePrice,
            RetailPrice = inputModel.RetailPrice,
            AidAmount = inputModel.AidAmount,
            Percentage = inputModel.Percentage,
            ProductionDate = inputModel.ProductionDate,
            ExpirationDate = inputModel.ExpirationDate
        };
        if (title == "Create")
        {
            var createdProduct = await ProductRepository.CreateProduct(product);
            await UpdateProductImage(product, Images);
            establishment_Product.ProductId = createdProduct.ProductId;
            var createdEstablishment_ProductRepository = await Establishment_ProductRepository.CreateEstablishment_Product(establishment_Product);
        }
        else
        {
            var createdProduct = await ProductRepository.UpdateProduct(product);
            await UpdateProductImage(product, Images);
        }
        NavigationManager.NavigateTo("/ProductList");
    }
}